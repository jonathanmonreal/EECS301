module reset_clock(clk, clk_out);

	input clk;
	output wire clk_out;
	
	reg [21:0] counter;
	assign clk_out = counter[n];
	
	always @(posedge clk)
		begin
			counter <= counter + 1;
		end
		
endmodule